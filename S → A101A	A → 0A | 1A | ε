#include <stdio.h>
#include <string.h>

int main() {
    char s[50];

    printf("Enter string: ");
    scanf("%s", s);

    if (strstr(s, "101"))
        printf("Accepted\n");
    else
        printf("Rejected\n");

    return 0;
}
